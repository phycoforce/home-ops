---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: llama-embed
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 1h
  values:
    defaultPodOptions:
      resourceClaims:
        - name: gpu
          resourceClaimTemplateName: litellm-llama-gpu
      hostIPC: true
    controllers:
      llama-embed:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          model-download:
            image:
              repository: python
              tag: 3.14-slim
            command:
              - /bin/sh
              - -ec
              - |
                set -euo pipefail

                if [ ! -s /models/llama-qwen-embed/Qwen3-Embedding-0.6B-Q8_0.gguf ]; then
                  pip install --no-cache-dir "huggingface_hub[hf_xet]"
                  mkdir -p /models/llama-qwen-embed
                  rm -f /models/llama-qwen-embed/.cache/huggingface/download/*.lock
                  hf download \
                    Qwen/Qwen3-Embedding-0.6B-GGUF \
                    Qwen3-Embedding-0.6B-Q8_0.gguf \
                    --local-dir /models/llama-qwen-embed

                  echo "Qwen3-Embedding-0.6B download complete"
                else
                  echo "Qwen3-Embedding-0.6B already downloaded, skipping"
                fi
            envFrom:
              - secretRef:
                  name: litellm-secret
            resources:
              requests:
                cpu: 250m
                memory: 512Mi
              limits:
                memory: 4Gi
        containers:
          app:
            image:
              repository: ghcr.io/ggml-org/llama.cpp
              tag: server-vulkan-b9755@sha256:e1f7fafab64479a05c700cf7ac6d1e04d2c5e891362c0b6feb52aac263faa4de
            securityContext:
              privileged: true
            args:
              - --host
              - 0.0.0.0
              - --port
              - "8080"
              - --alias
              - llama-embed
              - --model
              - /models/llama-qwen-embed/Qwen3-Embedding-0.6B-Q8_0.gguf
              - --embeddings
              - --pooling
              - last
              - --ctx-size
              - "32768"
              - --n-gpu-layers
              - "99"
              - --parallel
              - "2"
              - --cont-batching
              - --batch-size
              - "16384"
              - --ubatch-size
              - "16384"
              - --threads
              - "6"
              - --threads-batch
              - "12"
              - --metrics
              - --mmap
            env:
              ZES_ENABLE_SYSMAN: "1"
              ONEAPI_DEVICE_SELECTOR: level_zero:gpu
              UR_L0_ENABLE_RELAXED_ALLOCATION_LIMITS: "1"
              LD_LIBRARY_PATH: /app
            resources:
              claims:
                - name: gpu
              requests:
                cpu: 250m
                memory: 2Gi
              limits:
                memory: 4Gi
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  failureThreshold: 6
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  initialDelaySeconds: 20
                  periodSeconds: 10
                  failureThreshold: 6
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8080
                  failureThreshold: 360
                  periodSeconds: 10
    persistence:
      models:
        existingClaim: llama-embed
        globalMounts:
          - path: /models
      dev-dri:
        type: hostPath
        hostPath: /dev/dri
        globalMounts:
          - path: /dev/dri
    service:
      app:
        ports:
          http:
            port: 8080
