---
apiVersion: inference.llmkube.dev/v1alpha1
kind: Model
metadata:
  name: llama-rerank
spec:
  source: https://huggingface.co/ggml-org/Qwen3-Reranker-0.6B-Q8_0-GGUF/resolve/main/qwen3-reranker-0.6b-q8_0.gguf
  format: gguf
  quantization: Q8_0
  hardware:
    accelerator: vulkan
    gpu:
      enabled: true
      vendor: intel
      layers: -1
      resourceClaims:
        - name: gpu
          resourceClaimTemplateName: llmkube-llama-gpu
  resources:
    cpu: "250m"
    memory: 2Gi
---
apiVersion: inference.llmkube.dev/v1alpha1
kind: InferenceService
metadata:
  name: llama-rerank
  labels:
    krr/ignore: "true"
spec:
  modelRef: llama-rerank
  replicas: 1
  image: ghcr.io/ggml-org/llama.cpp:server-vulkan-b9837@sha256:dba48720a7fead3ae2a2a678e82a8db88bf5120db286cdf48c8461ad2c2d855b
  podLabels:
    igpu-workload: "true"
  topologySpreadConstraints:
    - maxSkew: 1
      topologyKey: kubernetes.io/hostname
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          igpu-workload: "true"
  contextSize: 16384
  parallelSlots: 2
  batchSize: 4096
  uBatchSize: 4096
  extraArgs:
    - --alias
    - qwen3-reranker
    - --rerank
    - --pooling
    - rank
    - --cont-batching
    - --threads
    - "6"
    - --threads-batch
    - "12"
    - --mmap
  env:
    - name: ZES_ENABLE_SYSMAN
      value: "1"
    - name: ONEAPI_DEVICE_SELECTOR
      value: level_zero:gpu
    - name: UR_L0_ENABLE_RELAXED_ALLOCATION_LIMITS
      value: "1"
    - name: LD_LIBRARY_PATH
      value: /app
  resources:
    cpu: 250m
    memory: 2Gi
  podSecurityContext:
    # No pod-level runAsNonRoot: the operator's model-cache-prep init container
    # must run as root (uid 0, CHOWN/FOWNER) to fix model-cache PVC perms.
    # The inference container is still enforced non-root via its own
    # container-level securityContext below.
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 102 # render GID; matches what the operator already injects
    supplementalGroups:
      - 44 # video
      - 102 # render -> grants open() on /dev/dri/renderD128
    seccompProfile:
      type: RuntimeDefault
  securityContext:
    allowPrivilegeEscalation: false
    runAsNonRoot: true
    runAsUser: 1000
    capabilities:
      drop:
        - ALL
    seccompProfile:
      type: RuntimeDefault
  endpoint:
    port: 8080
    type: ClusterIP
