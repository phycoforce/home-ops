---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/resource.k8s.io/v1/resourceclaimtemplate.json
apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: localai-gpu
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            deviceClassName: gpu.intel.com
