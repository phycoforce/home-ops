---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: palworld-dashboard-metrics
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
  storageClassName: miroir-replicated
