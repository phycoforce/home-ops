---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: volsync-system
components:
  - ../../components/alerts
  - ../../components/cluster-secrets
resources:
  - ./namespace.yaml
  # volsync + kopia UI removed after the kopiur migration; this namespace now
  # hosts only snapshot-controller (still required by kopiur's copyMethod: Snapshot).
  - ./snapshot-controller/ks.yaml
