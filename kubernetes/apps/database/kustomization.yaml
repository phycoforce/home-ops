---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: database
components:
  - ../../components/alerts
  - ../../components/cluster-secrets
resources:
  - ./namespace.yaml
  # DISABLED for OpenEBS→Miroir migration: CNPG torn down so its OpenEBS PVCs can be
  # deleted before the Micron repartition. Re-enable in Stage 4 (cluster.yaml is already
  # set to storageClass: miroir-local; re-bootstraps from barman on aionios MinIO).
  # - ./cloudnative-pg/ks.yaml
  - ./dragonfly/ks.yaml
